package circus.robocalc.robochart.impl;

public class RCPackageImplCustom extends RCPackageImpl {

// TODO: remove this. this case dealt with the derived attribute constants of the package.	
//	@Override
//	public EList<Literal> getLiterals() {
//		EList<Literal> literals = new BasicEList<Literal>();
//		EList<TypeDecl> types = this.getTypes();
//		if (types == null) 
//			return literals;
//		for (TypeDecl t: types) {
//			if (t instanceof Enumeration) {
//				literals.addAll(((Enumeration)t).getLiterals());
//			}
//		}
//		return literals;
//	}
}
